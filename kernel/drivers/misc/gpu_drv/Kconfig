#test for V_GPU
menuconfig V_GPU
    bool "Vivante GPU Support"

if V_GPU

config G2D 
    bool "G2D Support"

config G3D 
    bool "G3D Support"
    
config G2D_CLK_RATE
	int "G2D Clock Frequence"
	default "60000000"
	help
	  Set default G2D clock frequence
	  
config G2D_K3
    bool "K3 TESTCHIP POWER"

endif
